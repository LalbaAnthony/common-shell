# Concatenation order for ~/.bashrc_extra, top to bottom.
#
# One filename per line, relative to src/bash/. Blank lines and everything
# after a '#' are ignored. Filenames must not contain whitespace.
#
# The leading digit encodes shell coverage:
#   0NN  section exists in BOTH shells - the same 0NN number names the same
#        section in src/pwsh/, so a missing counterpart is visible at a glance
#   1NN  Bash only
#   2NN  PowerShell only (never appears in this file)
# Within a band the number steps by 10, leaving room to insert.
#
# Every *.sh file in this directory must be listed here exactly once:
# .github/scripts/check-manifests.sh fails the build otherwise, because an
# unlisted part is silently dropped from every install.

# -------- 0NN: shared with PowerShell --------
000-core.sh
010-self.sh
020-random.sh
030-network.sh
040-python.sh
050-node.sh
060-git.sh
070-claude.sh
080-docker.sh

# -------- 1NN: Bash only --------
100-apache.sh
110-sql.sh
120-certbot.sh
130-php.sh
140-laravel.sh
150-agoravita.sh
